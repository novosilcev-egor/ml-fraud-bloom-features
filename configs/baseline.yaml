# =====================================================================
# baseline.yaml — контрольный эксперимент.
# extends: base.yaml.
# =====================================================================

extends: base.yaml
experiment: baseline

# membership / frequency / агрегаты по сущностям в baseline запрещены —
# это исследуемый эффект этапов bloom/exact.
extra_features: {}
